apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    app: myapp
    location: amer
    type: frontend
spec:
  containers:
    - name: nginx-container
      image: nginx
    - name: redis-container
      image: redis
