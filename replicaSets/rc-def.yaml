apiVersion: v1
kind: ReplicationController
metadata:
  name: myapp-rc
  labels:
    app: myapp
    type: backend
spec:
  template:
    metadata:
      name: redis-pod
      labels:
        app: myapp
        type: backend
    spec:
      containers:
        - name: redis-container
          image: redis
  replicas: 2